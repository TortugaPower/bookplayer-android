<?xml version="1.0" encoding="utf-8"?>
<!--
    Release/default network security config. Cleartext (HTTP) is permitted GLOBALLY because
    Jellyfin/AudiobookShelf servers are arbitrary user-entered hosts (LAN NAS, Tailscale names)
    that very commonly run without TLS — same posture as iOS (NSAllowsArbitraryLoads=true),
    which shipped this way from day one. Blocking it broke media-server sign-in in production
    ("CLEARTEXT communication not permitted") while debug builds worked via the debug override.

    Our own API stays pinned HTTPS-only below — tighter than iOS: no config change or URL
    typo can ever downgrade BookPlayer account/library traffic to cleartext.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">api.bookplayer.app</domain>
    </domain-config>
</network-security-config>
