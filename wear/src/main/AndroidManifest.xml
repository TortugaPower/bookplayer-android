<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-feature android:name="android.hardware.type.watch" />

    <!-- Data Layer / node discovery in a later slice needs networked connectivity to the phone. -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Standalone (Pro) on-watch library sync runs in a foreground data-sync service; on-watch playback
         runs in a foreground media-playback service. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <!-- ExoPlayer holds a wake (and Wi-Fi, while streaming) lock during playback. -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- Show the ongoing sync notification (required to post notifications on API 33+). -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".WearApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.BookPlayerWear">

        <!-- Standalone: the watch app runs without a companion phone app being installed. The tier
             gate (pro = standalone, everything else = phone remote) is enforced in the app, not here. -->
        <meta-data
            android:name="com.google.android.wearable.standalone"
            android:value="true" />

        <activity
            android:name=".presentation.MainActivity"
            android:exported="true"
            android:taskAffinity=""
            android:theme="@style/Theme.BookPlayerWear">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- On-watch library sync engine (standalone/Pro). Not exported: started only in-process. -->
        <service
            android:name=".sync.WearSyncServiceHost"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- On-watch playback (standalone/Pro): the concrete MediaLibraryService PlaybackManager binds to.
             Exported + MediaSessionService/MEDIA_BUTTON intent-filter so the platform/media controllers can
             resolve it (no Android Auto browse action — that's phone-only). -->
        <service
            android:name=".service.WearPlaybackService"
            android:enabled="true"
            android:exported="true"
            android:foregroundServiceType="mediaPlayback">
            <intent-filter>
                <action android:name="androidx.media3.session.MediaLibraryService" />
                <action android:name="androidx.media3.session.MediaSessionService" />
                <action android:name="android.intent.action.MEDIA_BUTTON" />
            </intent-filter>
        </service>
    </application>
</manifest>
